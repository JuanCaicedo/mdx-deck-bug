import theme from 'mdx-deck/themes';

export default {
  // extends the default theme
  ...theme,
  // add a custom font
  font: 'Futura',
  // custom colors
  colors: {
    text: '#24292F',
    background: '#57C1FF',
    link: 'yellow',
    code: '#24292F',
    codeBackground: '#F3F4F4'
  }
};
